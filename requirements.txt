# pip-only mirror for inspection and tooling. The supported installation path is environment.yml.
# PyTorch is installed from Conda defaults by environment.yml. A CUDA override
# is optional and should be recorded separately for full LoRA training.
numpy>=1.26,<3
pandas>=2.2,<3
scipy>=1.12,<2
scikit-learn>=1.4,<2
matplotlib>=3.8,<4
seaborn>=0.13,<1
PyYAML>=6,<7
joblib>=1.3,<2
biopython>=1.83,<2
openpyxl>=3.1,<4
transformers>=4.44,<5
peft>=0.12,<1
accelerate>=0.33,<2
streamlit>=1.37,<2
py3Dmol>=2.4,<3
safetensors>=0.4,<1
pytest>=8,<9

# Optional upstream references, intentionally not installed in the reproducible
# environment because their current PyPI metadata pins obsolete NumPy/RDKit:
# modlamp>=4.3,<5
# cyclicpeptide>=1.4,<2
